/**
 * Disabled legacy worker to prevent double-processing of CallTasks.
 * Use src/lib/workers/task-runner.ts via /api/cron/process-calls instead.
 */
console.error("[CallXAI] Legacy worker is disabled. Use /api/cron/process-calls runner.");
process.exit(1);
